extends post.pug

prepend post
    // {block:Link}
append post
    // {/block:Link}

block content
    h3.card-header
        //- Is a literal, as it requires a weird interpolated block
        <a href="{URL}" {Target}>{Name}(This is a link post)</a>

    // {block:Description}
    .card-block
        p.card-text {Description}(This is what your link posts will look like)
    // {/block:Description}