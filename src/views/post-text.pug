extends post.pug

prepend post
    // {block:Text}
append post
    // {/block:Text}

block content
    h3.card-header
        a(href='{Permalink}') {Title}
    div.card-block
        p.card-text {Body}